const LovableAPI = require('./APIs/Lovable');
const CaptchaService = require('./APIs/CaptchaService');
const { apiStripe } = require('./APIs/StripePlan/index')
const { apiCheckout } = require('./APIs/StripeCheckout/index')

const dadosPagamento = {
    "codigos": ["SYMPOSIUMPC20"],
    "cartao": {
        "numero": "5226269772380877",
        "cvv": "922",
        "mesVencimento": "03",
        "anoVencimento": "34"
    },
    "endereco": {
        "nome": "Joao PIRES SILVA",
        "email": "joaodeprelian@gmail.com",
        "pais": "BR",
        "rua": "Rua Assef Buainain",
        "cidade": "Campo Grande",
        "estado": "MS",
        "cep": "79042-470"
    }
}

async function rodarTeste() {

    let conta = {}
    conta.id = ""
    conta.email = "mbrxrubwb3691@hotmail.com"
    conta.senha = "fq4JBBWdbFm"

    let Token = 'eyJhbGciOiJSUzI1NiIsImtpZCI6IjczMmNhOTY3MTNiMWRkMTcyMzg1MDg0Y2U5ZjQzODFhZDAwY2VjZTQiLCJ0eXAiOiJKV1QifQ..wz8SfdSc5xpxDA9TTUWFow31UhHWYJT_RFAVIQI25jW-O_I95MUrOolAUd5HYyQOZHZZfpcSAUr_xtgZTPhDNKwOgY5eNzEb37h5jBjoKn5P8nAr0FTWoJ_hNlClted_WfdvuBGzXoqveSLtwTGfGZnTyxXeshCJgC5NkRWG4nKNkSdlhpB8rHaX6sVOp8J9Knu3ktHeKBGwZ7wbCgRw8VyHCIB_6uFfcZXwqIckR8eWofCmArcclnI8aPkSr3CQ9_TFXHuxdDLZXqCYHPhOVSsTIBNFHXjBCwcXwH3wQyxfRIIhwuzkETKDTqWEa8Dun8cYf88XZW2rfMiE8wVvbg'
    let login = ''

    if (!Token) {
        login = await LovableAPI.login('', conta.email, conta.senha)
        console.log(login)
        Token = login.dados.idToken
        console.log("Token: ", Token)
    }

    // Loop para repetir o ciclo 25 vezes
    for (let i = 1; i <= 25; i++) {
        console.log(`\n🔄 Iniciando ciclo ${i} de 25...`);

        try {
            const workspacesRes = await LovableAPI.listarWorkspaces('', Token);

            // Encontra o workspace onde o usuário é dono (permissão 'owner')
            let workspaceOwner = workspacesRes.dados.find(wp => wp.membership.role === 'owner');

            if (!workspaceOwner) {
                console.log("❌ Nenhum workspace OWNER encontrado. Pulando ciclo.");
                continue;
            }

            let Saldo = await LovableAPI.getSaldoCreditos('', Token, workspaceOwner.id)

            if (Saldo.total_granted >= 200) {
                return
            }

            let Plan = ''

            console.log(`Plano atual: ${workspaceOwner.plan}`);

            if (workspaceOwner.plan == "free" || workspaceOwner.plan == "ktlo_2") {
                Plan = await LovableAPI.gerarCheckout("", Token, workspaceOwner.id, 'monthly_pro_20')
            } else {
                Plan = await LovableAPI.gerarCheckout("", Token, workspaceOwner.id, 'ktlo_2')
            }

            let resultado

            if (workspaceOwner.plan == "free") {
                resultado = await apiCheckout.processarPagamento(Plan.dados.checkoutUrl, dadosPagamento)
            } else {
                resultado = await apiStripe.mudarPlano(Plan.dados.checkoutUrl)
            }

            console.log(`✅ Resultado do ciclo ${i}:`, resultado);

        } catch (error) {
            console.error(`⚠️ Erro no ciclo ${i}:`, error.message);
        }

        // Intervalo entre 5 a 10 segundos (exceto no último ciclo)
        if (i < 25) {
            const tempoEspera = Math.floor(Math.random() * (5000 - 1500 + 1) + 1500);
            console.log(`Waiting ${tempoEspera / 1000}s para o próximo ciclo...`);
            await new Promise(resolve => setTimeout(resolve, tempoEspera));
        }
    }

    console.log("\n🏁 Todos os 25 ciclos foram concluídos.");
    
}

rodarTeste();